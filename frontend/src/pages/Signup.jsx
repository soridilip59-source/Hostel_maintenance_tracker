import {useState} from "react";import {useNavigate} from "react-router-dom";import api from "../services/api";import AuthShell,{PasswordField} from "./AuthShell";import "./Login.css";
export default function Signup(){const n=useNavigate(),[name,setName]=useState(""),[email,setEmail]=useState(""),[password,setPassword]=useState(""),[confirm,setConfirm]=useState(""),[error,setError]=useState(""),[show,setShow]=useState(false),[showConfirm,setShowConfirm]=useState(false),[busy,setBusy]=useState(false);const submit=async e=>{e.preventDefault();if(!name||!email||!password||!confirm)return setError(!name?"Name is required":!email?"Email is required":!password?"Password is required":"Please confirm your password");if(password.length<6)return setError("Password must contain at least 6 characters");if(password!==confirm)return setError("Passwords do not match");setBusy(true);try{await api.post("/auth/register",{name,email,password});n("/")}catch(x){setError(x.response?.data?.message||"Server is not running")}finally{setBusy(false)}};return <AuthShell mode="signup" title="Create account" subtitle="Join your hostel community in a few simple steps." error={error} footer={<>Already have an account?</>}><form className="auth-form" onSubmit={submit}><div className="auth-field"><label htmlFor="name">Full name</label><div className="auth-input-wrap"><span className="auth-input-icon" aria-hidden="true">👤</span><input id="name" value={name} onChange={e=>setName(e.target.value)} placeholder="Your full name" required/></div></div><div className="auth-field"><label htmlFor="signup-email">Email address</label><div className="auth-input-wrap"><span className="auth-input-icon" aria-hidden="true">✉</span><input id="signup-email" type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="you@hostel.edu" required/></div></div><PasswordField id="signup-password" label="Password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="At least 6 characters" visible={show} onToggle={()=>setShow(!show)}/><PasswordField id="confirm-password" label="Confirm password" value={confirm} onChange={e=>setConfirm(e.target.value)} placeholder="Repeat your password" visible={showConfirm} onToggle={()=>setShowConfirm(!showConfirm)}/><button className="auth-submit" disabled={busy}>{busy?"Creating account…":"Create account"}<span>→</span></button></form></AuthShell>}
