import { useNavigate } from "react-router-dom";

export function PasswordField({ id, label, value, onChange, placeholder, visible, onToggle }) { return <div className="auth-field"><label htmlFor={id}>{label}</label><div className="auth-input-wrap"><span className="auth-input-icon" aria-hidden="true">🔒</span><input id={id} type={visible ? "text" : "password"} value={value} onChange={onChange} placeholder={placeholder} minLength="6" required /><button className="password-toggle" type="button" onClick={onToggle} aria-label={visible ? "Hide password" : "Show password"}>{visible ? "Hide" : "Show"}</button></div></div>; }
export default function AuthShell({ mode, children, title, subtitle, error, footer, roleSelector, google, loading }) {
  const navigate = useNavigate(); const signup = mode === "signup";
  const changeMode = () => navigate(signup ? "/" : "/signup");
  return <main className={`auth-page ${signup ? "is-signup" : "is-login"}`}><div className="auth-orb auth-orb-one"></div><div className="auth-orb auth-orb-two"></div><div className="auth-grid"></div><section className="auth-card" aria-label={signup ? "Create an account" : "Sign in"}><aside className="auth-visual"><img className="hostel-photo" src="/assets/boys-hostel.jpeg" alt="NavGurukul boys hostel" /><div className="photo-wash"></div><div className="visual-copy"><span className="visual-logo"><img src="/assets/navgurukul-logo.png" alt="NavGurukul" /></span><p>Hostel maintenance tracker</p></div><div className="visual-caption"><strong>Comfortable living starts with a well-cared-for home.</strong><span>Report, track, and resolve hostel maintenance in one place.</span></div></aside><section className="auth-form-panel"><div className="mobile-brand"><img src="/assets/navgurukul-logo.png" alt="NavGurukul" /></div><div className="auth-heading"><p className="eyebrow">{signup ? "GET STARTED" : "WELCOME BACK"}</p><h1>{title}</h1><p>{subtitle}</p></div>{roleSelector}<div className="auth-form-content">{children}</div>{error && <p className="auth-error" role="alert">{error}</p>}{google}{loading && <p className="auth-status">Signing you in with Google…</p>}<p className="auth-footer">{footer} <button type="button" onClick={changeMode}>{signup ? "Sign in" : "Create account"}</button></p><p className="auth-credit">Web created by Aman & Dilip</p></section></section></main>;
}
